# UI System

## Design + Shadcn Palette

- Theme tokens: use the existing tokens; `--primary` is the impulse accent.

## Component Usage Rules

- Card: KPI + chart containers.
- Dialog: logging flow.
- Button variants: `primary`, `ghost`.
- Typography: Inter + Fraunces (optional for headings).
